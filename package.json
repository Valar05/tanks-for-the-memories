{
  "name": "tftm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && node scripts/build.mjs",
    "preview": "vite preview",
    "smoke": "node scripts/smoke.mjs",
    "compiler:test": "node --import tsx scripts/test-command-compiler.mts",
    "browser:acceptance": "node scripts/browser-acceptance.mjs",
    "audio:generate": "node scripts/generate-cartesia-audio.mjs",
    "bootstrap": "sh scripts/bootstrap.sh"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "esbuild": "^0.28.1",
    "playwright-core": "^1.61.1",
    "tsx": "^4.20.3",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
