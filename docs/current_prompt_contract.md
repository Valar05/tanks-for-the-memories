# Current Prompt Contract

latest_user_command: Implement chassis silhouette smoke and fit the chassis silhouette after v1.1 read as a tent.
controlling_user_correction: The v1.1 chassis is visual-red because it is not a tank; it reads as a tent/canopy. Before rebuilding, the failure mechanism must be guarded by a silhouette smoke that rejects the tent shape, then the chassis must be fitted to a Sherman-like hard-surface silhouette while leaving golden tread v1.8c untouched.
forbidden_stale_premise: Any new chassis export that relies only on watertight/raycast/bounds checks, preserves the lofted center-ridge tent cross-section, mutates the golden tread asset, exports Meshy topology, adds turret/barrel/wheel/tread geometry, wakes browser for discovery, or claims visual success without cloud/Sense review.
allowed_mutation_type: doctrine_update; asset_export; runtime_review_surface; validation_gate; cloud_visual_release; visual_qa_gate; cloud_review_deploy
allowed_target_artifact: authored_sherman_chassis_v1 v1-2 silhouette-fit watertight chassis shell, chassisfirst-chassis review surface composed with read-only authored_sherman_treads_v1 v1-8c linked mirrored golden tread reference, and silhouette smoke diagnostics that fail v1.1 tent geometry.
required_evidence_lane: add silhouette smoke that fails v1.1 tent geometry, export chassis v1-2 without changing golden tread files, prove one visible watertight chassis mesh with broad flat deck, vertical side armor, non-tent profile, no forbidden turret/barrel/tread/wheel nodes, raycast chassis/tread interface against frozen tread bounds, rebuild cloud release, run chassis cloud gate, deploy, verify hosted chassis bundle and golden tread tokens; visual acceptance still requires cloud/Sense review.
updated_at: 2026-07-05
